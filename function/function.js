// Creating a function

function sayMayName(name)
{
   console.log('Your Name Is: ' + name);
}

// Calling a function
sayMayName('Welton');

// Other exemple, here I multiplied two values ​​and returned the result.

 function square(value)
 {
    return value * value;
 }

// I stored the value (that my function returned) in the variable
 const squareOf10 = square(10);
 console.log(squareOf10);
